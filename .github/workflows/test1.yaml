
name: Test Workflow

on: Push

jobs:
   test-job:
      runs-on: ubuntu-lates

      steps:
        -name: Hello Display
        run: echo "hello sayed Kareem"
        
        -name: Display Date 
        run: date

